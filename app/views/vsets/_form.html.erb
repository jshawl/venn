<%= form_for(@vset) do |f| %>
  <% if @vset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vset.errors.count, "error") %> prohibited this vset from being saved:</h2>

      <ul>
      <% @vset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
