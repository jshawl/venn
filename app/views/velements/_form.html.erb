<%= form_for(@velement) do |f| %>
  <% if @velement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@velement.errors.count, "error") %> prohibited this velement from being saved:</h2>

      <ul>
      <% @velement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
